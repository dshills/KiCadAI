Create a two-layer 70 mm by 45 mm catalog-resolved LM358 buffered AC-coupled signal-conditioner board with `erc-drc` acceptance. Use one physical TI LM358DR SOIC-8 package and expose all three verified KiCad symbol units: unit A as a 2.5 V reference buffer, unit B as a non-inverting gain stage, and unit P for the shared supply pins. Emit one component, one reference U1, one footprint, and units A, B, and P; do not model the two amplifiers as separate packages.

Provide two-pin connectors for 5 V power, signal input, and signal output. Derive 2.5 V VREF_RAW from 5 V with two equal 100 kOhm resistors and bypass it to ground with 1 uF. Connect unit A as a voltage follower: VREF_RAW to A.IN_PLUS and A.OUT directly to A.IN_MINUS. Name the buffered rail VREF. AC-couple the signal input with 1 uF and bias unit B IN_PLUS to VREF through 100 kOhm. Configure unit B for non-inverting gain with 100 kOhm feedback from B.OUT to B.IN_MINUS and 10 kOhm from B.IN_MINUS to VREF. AC-couple B.OUT to the output connector with 1 uF and reference the connector side to ground through 100 kOhm. Add one 100 nF local supply-decoupling capacitor at the physical LM358 package.

Use only trusted catalog components and functions. Declare exactly one selection mechanism per component. Use `opamp.ti.lm358.soic8`, variant `soic8`, manufacturer `Texas Instruments`, MPN `LM358DR`, and required functions IN_MINUS, IN_PLUS, OUT, V_MINUS, and V_PLUS. Unit-qualify every LM358 net endpoint. Include all units A, B, and P explicitly. On the LM358 component, set each of these properties to exactly `review_required`: `stability_status`, `gain_bandwidth_status`, `output_swing_status`, `input_common_mode_status`, `output_drive_status`, `noise_status`, `distortion_status`, and `load_compatibility_status`.

Lay out the schematic left to right with input on the left, unit B in the center, and output on the right. Keep unit A and the VREF divider near the analog stage, unit P and positive supply above, ground below, feedback adjacent to unit B, and decoupling adjacent to the package supply pins. Center the schematic and prefer labels for shared power and VREF nets.

Use bounded power-input, signal-input, analog-core, and signal-output PCB regions. Place one U1 footprint in the analog core, input connectors on the left edge, and the output connector on the right edge. Keep the supply decoupling within 3 mm of U1, feedback and gain resistors within 3 mm of U1, and VREF bypass within 3 mm of the divider. Express placement through generic regions and relative constraints, not fixed coordinates. Require all nets to be physically routed and do not add a copper zone.

ERC and DRC do not establish analog performance. Do not claim fabrication readiness, stability, gain-bandwidth, output swing, input common-mode compliance, output drive, noise, distortion, or load compatibility.
