Create a two-layer 80 mm by 50 mm catalog-resolved LMV321 AC-coupled signal-conditioner board that must pass KiCad ERC and strict DRC. Set project acceptance to exactly `erc-drc`; do not claim `fabrication_candidate` acceptance.

Use only trusted catalog components and functions. For every component, declare exactly one selection mechanism: either `component_id` or `query`, never both. Use only the existing generic component-role vocabulary: `ic` for each LMV321, `input_connector` and `output_connector` for connectors, `resistor` for resistors, `capacitor` for the AC-coupling capacitors, and `decoupling_capacitor` for the shared VREF bypass and each amplifier's local 100 nF supply bypass. Do not label the VREF bypass as `bulk_capacitor`. Provide a two-pin 5 V power input, a two-pin signal input, and a two-pin signal output. Use two distinct instances of the verified TI LMV321 SOT-23-5 component. Preserve both amplifier identities, symbols, footprints, and pin mappings independently.

Generate a shared 2.5 V virtual reference from 5 V using two 100 kOhm resistors and a 1 uF ceramic bypass capacitor. AC-couple the input with 1 uF and bias the first non-inverting input to VREF through 100 kOhm. Configure each LMV321 as an independent non-inverting stage with 100 kOhm feedback from output to inverting input and 10 kOhm from inverting input to VREF, for nominal gain near 11 per stage. Connect the first stage output directly to the second non-inverting input. AC-couple the final output with 1 uF and reference the connector side to ground through 100 kOhm. Use existing trusted 0805 catalog variants for every resistor and for the 1 uF coupling and VREF capacitors. Place a separate verified 0603 100 nF local supply-decoupling capacitor at each amplifier.

Make 5V, GND, VREF, SIGNAL_IN, STAGE1_IN, STAGE1_FB, STAGE1_OUT, STAGE2_FB, STAGE2_OUT, and SIGNAL_OUT required nets. Use 0.5 mm tracks for 5 V and ground and 0.25 mm tracks for analog signals. Mark 5 V and ground as externally driven power nets.

The schematic must flow left to right: input, first gain stage, second gain stage, output. Put positive power above, ground below, shared VREF near both stages, each feedback network adjacent to its owning amplifier, and each decoupling capacitor adjacent to its corresponding power pins. Orient passives that connect vertically between a power rail, VREF, ground, or a stage node as `rotated_90`: the VREF divider and bypass, each amplifier's gain-to-VREF resistor and decoupling capacitor, the input bias resistor, and the output ground-reference resistor. Keep AC-coupling capacitors and output-to-input feedback resistors horizontal with `normal` orientation. Reserve each amplifier's `above` relation for its local decoupling capacitor; feedback, gain, bias, and shared VREF parts may be `near` but must not also use `above` with that amplifier. Emit exactly one schematic placement for each component. In schematic placement relations, `near`, `above`, and `right_of` must reference component IDs only, never group IDs. Keep the drawing centered, bounded, and readable.

Place the power and signal inputs toward the left, the first amplifier before the second, and the output toward the right. In PCB placements, explicitly set `edge` to `left` for both input connectors and `right` for the output connector; region names alone are not sufficient interface anchors. Use one shared, non-overlapping analog-core placement region for both amplifiers, both feedback networks, both decoupling capacitors, and the shared VREF components; keep only the input and output interfaces in separate left and right regions. Do not create separate or overlapping stage and VREF regions. PCB placement dependencies must be acyclic: make the first amplifier an independent placement anchor with no proximity dependency on the second amplifier, then place the second amplifier near the first. Do not create mutual or circular `near` constraints. Use these exact generic priority tiers: `120` for all three edge-anchored interface connectors, `110` for the first amplifier, `100` for the second amplifier, `90` for AC-coupling capacitors, each amplifier's direct feedback/gain/bias/decoupling support, and the VREF divider top, and `80` for the output ground-reference resistor plus the VREF divider bottom and VREF bypass. These tiers are topological: every anchor is strictly higher than each component that names it in `near`. Route the shared 5 V, ground, and VREF trees plus both feedback loops and the inter-stage signal deterministically. Do not add a copper zone.

ERC and DRC do not prove analog performance. On each LMV321 component, emit a distinct `stage` property (`1` or `2`) and all six structured review properties with the exact value `review_required`: `stability_status`, `gain_bandwidth_status`, `output_drive_status`, `noise_status`, `distortion_status`, and `load_compatibility_status`. Do not replace these properties with prose in the project description. This is a small-signal conditioner, not a headphone or power-amplifier output.
