{
  "id": "i2c_sensor_breakout_candidate",
  "request": "i2c_sensor_breakout_candidate.json",
  "tier": "block-composition",
  "readiness": "expected_fail",
  "acceptance": "erc-drc",
  "require_erc": true,
  "require_drc": true,
  "allowlists": [],
  "expected_artifacts": [
    ".kicadai/design-promotion.json"
  ],
  "expected_stages": [
    "block_planning",
    "component_selection",
    "schematic",
    "schematic_electrical",
    "pcb_realization",
    "placement",
    "routing",
    "project_write",
    "writer_correctness",
    "validation",
    "kicad_checks"
  ],
  "known_gaps": [
    "Routing is now enabled and bounded placement retry legalizes the generated sensor, pull-up, decoupling, and connector placements, but inter-block route completion still blocks promotion.",
    "Block-local route net names are rewritten to composed aliases so VCC/GND/SDA/SCL local route contact proof no longer fails on sensor_* instance-net mismatches.",
    "Current route-tree repair evidence manages all 4 VCC/GND/SDA/SCL nets, excludes them from fallback net-level routing, classifies branch/contact failures, and feeds repairable route-tree hints into bounded placement retry.",
    "The latest run selected retry attempt 2 after a spacing adjustment because route-tree proven endpoints improved from 10 to 11. The fixture still blocks on VCC route-completion/contact evidence: VCC reports one ROUTE_GRAPH_INCOMPLETE contact proof plus two branch-scoped no-legal-two-layer-path blockers, including one blocked near another net's decoupling pad.",
    "Route-tree contact graph evidence now reports 3 complete contact-graph groups, 1 partial contact-graph group, 0 blocked contact-graph groups, 11 proven endpoints, and local-route merge evidence for the managed VCC/GND/SDA/SCL nets. Route-tree diagnostics now separate 24 fixed-net preservation notices and 4 missing-net-class warnings from 10 blocking route-tree issues; repair hints narrow to 3 VCC failures.",
    "Project write, writer correctness, validation, and KiCad checks remain skipped until the routing stage emits complete same-net contact graph evidence for every required I2C breakout endpoint."
  ],
  "notes": "Tracks the richer sensor breakout scenario after placement and block-local net-alias blockers; the remaining expected failure is multi-endpoint inter-block route completion."
}
