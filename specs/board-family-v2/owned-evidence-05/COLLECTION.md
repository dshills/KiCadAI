# Owned-v4 collection checkpoint 05

Status: **offline rehearsal passed; the project goal remains active**.

The subsequent [scoring checkpoint](SCORING.md) adds raw-v4 assessment and a
separately bound review without rerunning this collection or native generation.

This extends the integrated route in [INTEGRATION.md](INTEGRATION.md). It does
not change the production extractor, model, admission rules, circuit templates
or historical evaluation. The only Go change extends a test-only subprocess
helper; no fixture flag or alternate endpoint is shipped.

## Verified result

- All 14 unchanged, known regression prompts completed through real command
  processes, using hand-authored synthetic responses over an in-memory HTTP
  transport. Every selection and ledger replayed through the owned-v4 auditor.
- All five supported configurations generated complete native bundles with real
  KiCad 10.0.3 validation. 201 native, preview and manufacturing files matched
  the existing reviewed examples using the existing explicit normalization rules.
- The retained collection took 33.926 seconds, including source rechecks,
  contract checks, command processes, replay audits and native comparisons.
  This is an offline harness measurement, **not model latency or accuracy**.
- Five affected Go packages passed short tests; boardfamily and its command
  passed race tests; repository-wide lint reported zero issues.
- 74 unchanged legacy safeguard tests passed. The new safeguard suite passed
  16 checks, with its one optional native case intentionally skipped there:
  the retained 14-case rehearsal separately ran all five native bundles.
  An earlier standalone native integration test also passed.
- Completed invalid JSON/extraction and provider refusals remain measured
  failures. Transport/metadata failures, crashes, timeouts, oversized logs,
  generation conflicts and failed native validation stop before the next case.
  Existing user-owned output survives a generation conflict.
- Changed contracts (even with recomputed hashes), changed bytes/accounting,
  process records, outcome records, extra files and wrong protocol/manifest
  versions are rejected by the relevant checks. Reusing a batch directory fails
  without changing prior accounting.

These are synthetic pipeline tests, not 14 new successful AI answers. The last
live batch is still 9/14 complete, with 3/5 required native bundles. No archived
answer was edited, migrated, repaired, refunded or rescored.

## What the versioned package contains

The new collector is explicitly **offline-only** and requires the compiled
test helper with in-memory transport. It rejects live mode and approval files.
Provider credentials and old/new test override variables are stripped from
children. It imports unchanged process/accounting helpers, not the indexed-v3
collector's protocol selection or semantic interpretation.

Each original prompt has its own contract exported by the pinned production
binary. Before rehearsal, these contracts are regenerated by the pinned test
binary and compared exactly. They are never replaced with a static v3 schema.
The contract exporter and journal replay do not access a provider.

The append-only preparation records 7,350 source/data files, 2,113 external
compiler/tool inputs, executable hashes, actual terminal process results and
logs. The broad source snapshot includes runtime-read archived fixtures and
reviewed native references. New checkpoint Markdown is explicitly excluded
because it is not an evaluation input. The preparation's source_commit is the
checkout base; the full content hashes bind the actual working source.

Reviewed board qualification is reused unchanged and authenticated, including
the reviewed example inventories and historical bindings. This is local byte
consistency and process evidence, not independent provider attestation,
independent semantic review, fabrication or bench testing.

Local retained package (ignored by Git):

```text
.cache/board-family-v2/owned-rehearsal-05-01/
```

Manifest SHA-256:
`26ef434867fa660d259d910a61373da9a0bf0c69f6c2f0a023061b881b4dfe98`

Result SHA-256:
`61f4609e59da47abd612b1812423fda8f9b3adf6ab51dc2ca8cfb10015582160`

Read-only verification from this worktree, with provider credentials removed:

```sh
node specs/board-family-v2/owned-evidence-05/prepare-rehearsal.mjs \
  --check .cache/board-family-v2/owned-rehearsal-05-01
```

A new CI workflow exercises the portable, offline contract/process checks. It
has not run remotely: this checkpoint remains local and unpublished. Native
qualification still requires the pinned macOS KiCad installation.

## Remaining gates

1. Complete the evaluator-only owned-v4 raw-fact scoring and source-bound
   semantic review contract. Preserve raw v4 facts; application-owned compiled
   admission input must never masquerade as model output.
2. Bind that reviewer/scorer addition separately to the unchanged runtime and
   rehearsal evidence so native qualification need not be repeated gratuitously.
3. Finish exact-source review and CI before preparing the final live collector
   and obtaining fresh, specific budget authority. This rehearsal manifest
   cannot be promoted to live use by changing a status or adding an approval.
4. Only then evaluate actual model extraction and report raw, application and
   complete success separately. The corpus is known regression data, not an
   independent holdout.

Zero live requests, external model reviews, firewall changes, fabrication or
bench actions occurred at this checkpoint. Prior batch04 authority remains
exhausted. The primary checkout and PR are unchanged.
