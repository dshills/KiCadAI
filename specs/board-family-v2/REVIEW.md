# Final evaluation review — release blocked

Reviewer: implementing Codex agent, September 14, 2026; **not independent**. Assessment: **share the failed results with caveats; do not promote the natural-language workflow**. Full evidence is in [RESULTS.md](RESULTS.md), the [case-by-case semantic review](evaluation/SEMANTIC-REVIEW-01.json), and the [authenticated publication](evidence/final-01/manifest.json).

## High-priority findings

1. **P1 — Unsupported heater requirement reaches native generation.** In `unsupported-04`, the model says continuous heater operation during humidity measurement is allowed firmware behavior. The fixed SHT31 family requires heater off, but the application accepts a valid nine-field numeric configuration and produces complete native/manufacturing outputs. `DecodeDecision`'s supported branch checks clause labels, numeric configuration, geometry and low-current scope; it does not independently establish compliance with the heater condition. All 14 CAD/export gates pass because they test the generated reference, not satisfaction of that unmodeled behavior. This is a release blocker, not a cosmetic explanation issue. Do not use this failed output for fabrication or claim the request is supported.
2. **P1 — Model explanations can override or invent engineering capability.** `unsupported-02` explicitly acknowledges 70 pF but treats the user's 100 pF request as an override; numeric checking blocks it. `unsupported-05` silently invents regulation to accept direct 5 V USB and only refuses wireless. That command is rejected for malformed original text, not proof that every semantic capability violation is caught. These caught cases cannot justify trusting free-form reasoning for all admission decisions.
3. **P2 — Contradictory schema fields and fragile source-text reconstruction reduce completion.** `supported-02` duplicates a negated requirement; `clarify-01` and `unsupported-06` combine a non-design disposition with supported clauses and non-null configurations. Three otherwise correct supported raw responses lose boundary spaces; one clarification reorders clauses. The app repairs some provenance discrepancies, but five commands fail, and only 4/14 raw responses satisfy the full frozen contract. Keep raw and local outcomes separate; do not weaken the exact-text criterion retroactively.

## Methodology and verification

- All 14 original prompts were reviewed against both full decision forms, their explicit case conditions, stderr/stdout and output/no-output behavior. A relevant message alone does not pass when configuration or clause labels contradict it.
- All 14 physical reservations completed with distinct response IDs and matching positive usage. Per-request upward-rounded costs were recomputed from input/output tokens. The exhausted v1 ledger was unchanged. No retries, probes, additional baseline, missing outcome or changed denominator.
- Five stops and four resumptions are preserved. Each resumption checked terminal processes, unchanged prior output hashes/ledger, absent pending paths and the exact unseen suffix. The final state remains `stopped` because case 14 failed; publication distinguishes complete collection from successful acceptance.
- The four legitimate native bundles and one wrongly admitted bundle pass 14/14 native checks each. All 204 corresponding deliverable comparisons pass under unchanged normalization. The latter is evidence of reproducible CAD, not semantic correctness. No evaluated output was repaired.
- The post-run audit recomputes metrics from retained selections even where a CLI error prevented the original runner from assigning automatic grades. It never edits the runner's frozen state. Manual verdicts are source-hash bound and deliberately not presented as an independent review.
- Timing includes the failed supported attempt: median 8.653 s, maximum 9.602 s over all five. Completion quality remains 8/14. No charts are needed; exact case and timing tables retain the full population without selective denominators.
- Runtime `ab1a224d` already passed full local tests, zero-issue lint, 17 offline runner tests, and [25/25 CI jobs](https://github.com/dshills/KiCadAI/actions/runs/34780151432), including the actual 79.10% generated-excluded coverage gate against 75.00%. That proves software checks, not language acceptance. The evidence-publication commit requires its own fresh CI before a new-head green claim.

## Next engineering direction — not an approved new live batch

Keep the qualified CAD/reference pipeline and replace redundant model-owned decisions with a smaller typed intent representation. Preserve the original prompt verbatim in application-owned storage rather than asking the model to recreate it. Derive disposition and configuration/nullability once, in code; explicitly model required, excluded, unknown and conflicting capabilities such as heater operation, USB input, wireless, accuracy and both-sensor integration. If a mandatory requirement cannot be safely mapped to a supported capability, ask a targeted question or refuse. This design still needs adversarial offline tests; it is not a claim that deterministic parsing of arbitrary language is solved.

First add offline regression cases for all failures recorded here, plus paraphrases, negations and conflicting requirements, before considering a separately frozen successor evaluation. Do not silently clear non-null fields, flip clause labels, repair this batch or add a one-string heater workaround and call the broader admission problem solved.

## Handoff blockers and caveats

The required 14/14 live acceptance failed, including one semantically unsupported generated board. The original request budget is exhausted. The goal is not complete and PR #14 remains draft; fixing and proving a successor requires a separately identified change/evaluation, not reuse of these first attempts. No extra API request, recovery baseline, model change, Gemini review, fabrication, firmware or bench work has been authorized here.

Hash verification establishes local byte provenance, not an external attestation or billing audit. The existing electrical/assembly/manufacturing caveats remain: software-only validation, unmeasured controller power/heat and assembled ambient accuracy, heater-off/CRC/firmware requirements, sparse silkscreen and assembler orientation confirmation. Physical bring-up remains a separate milestone.
