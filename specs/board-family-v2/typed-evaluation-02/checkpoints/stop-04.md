# Stop 04 and approved continuation

The continuation completed useful-05, choice-01 and choice-02, then exited 1. Useful-05 generated the expected validated SHT31/fast bundle. Choice-01 wrongly labeled `indoor project` as custom_geometry and refused. Choice-02 correctly identified uncertain alternatives in clause 0 but repeated named-sensor facts against a pronoun-only clause; source identity validation rejected these. Its generic extraction-error clarification is not the targeted family question required by the case.

The read-only audit passed evidence/accounting at seven completed responses, 350000 micro-USD reserved and 8588 micro-USD estimated. Its independently recomputed automatic raw count is four; the runner count is three because it skips scoring after a nonzero child exit. Neither count establishes semantic accuracy. All discrepancies remain visible.

Terminal children are recorded; `ps -p 63111,63168` returned no processes. State and ledger were copied before continuation. Existing APPROVAL-02.json permits untouched-only resume starting at choice-03. Seven slots remain; no previous case is retried or repaired.
