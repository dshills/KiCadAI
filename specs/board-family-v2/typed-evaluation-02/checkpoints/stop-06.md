# Stop 06 and approved continuation

The continuation exited 1 after refuse-02. The response correctly records initial quantities but repeats profile/numeric facts against `that profile` / `all those values` rather than source-literal identities and quantities. The application rejects the evidence and gives a generic clarification instead of explaining the unsupported SHT31 standard/100 pF request.

Read-only audit passed integrity/accounting with ten completed responses, 500000 micro-USD reserved and 12296 micro-USD estimated. All terminal children are recorded; `ps -p 64052,64068` returned no processes. State/ledger were copied before continuation.

Existing APPROVAL-02.json permits untouched-only resume from refuse-03. Four physical slots remain; no retry, input change or additional budget.
