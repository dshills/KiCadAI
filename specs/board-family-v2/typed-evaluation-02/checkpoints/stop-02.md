# Stop 02 and approved continuation

The continuation exited 1 after useful-03. The accounted model response invented numeric supply/ambient facts from a defaults-only clause and used forbidden instead of not_required for the excluded power/battery requirements. The application rejected the ungrounded numbers and generated no board. This is preserved failed evidence, not a transport or billing error.

The read-only audit passed all integrity/accounting checks with three completed responses, 150000 micro-USD reserved and 3888 micro-USD estimated. Terminal child observation is recorded; `ps -p 62118,62131` returned no processes. State and ledger were copied before continuation.

Existing APPROVAL-02.json covers untouched-only continuation. Resume starts at useful-04 with 11 physical slots remaining. No prior case is retried and no frozen input or previous evidence is changed.
