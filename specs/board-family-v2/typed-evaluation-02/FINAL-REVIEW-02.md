# Final review: keep PR #14 draft

## Release assessment

**Acceptance failed.** The two-family deterministic construction and complete native bundles remain qualified, but this frozen live language workflow does not meet the practical reliability goal. The independent raw/application distinction matters: raw **5/14**, application **7/14**, complete **5/14**, useful generation **2/5**. Do not mark the milestone achieved or merge as a finished autonomous language-to-board feature.

## Prioritized findings

1. **High — brittle source-evidence contract rejects useful requests.** Six provider responses trigger local extraction validation errors: non-verbatim quotes, invented numeric defaults, ungrounded pronoun references and duplicate clause IDs. Three are useful requests. The no-retry stop behavior preserves safety/accounting but causes six human-agent verification/resume cycles. All failures are reproducible from recorded selections without new API spend.
2. **High — valid shape/quote is not valid meaning.** choice-01 invents custom_geometry from indoor context and the application issues an irrelevant refusal. refuse-04 weakens a startup prohibition; refuse-05 invents a GPIO-load prohibition from a no-adapter/no-change condition. The latter two are correctly refused for other reasons, but the raw extraction still fails. Source quotes and enums cannot by themselves certify semantic completeness.
3. **Medium — fallback questions are not targeted clarification.** The six validation errors produce the same generic extraction-error message. This avoids generating a mismatched board but does not satisfy the frozen targeted-question/refusal criteria. No generic safe refusal has been promoted to a success.
4. **Medium — child time hides operational delay.** Summed child time is 45.228 s, while complete collection takes 467.749 s including verified resumptions. Median useful-attempt time passes only when failures are retained and labeled; it is not proof of generating all five boards quickly.

Confidence is high for these exact recorded counterexamples, not a population failure-rate estimate. No change to production admission, model, payload, schema, gates or evaluated artifacts was made during this run.

## Evidence-quality validation

**Share with caveats as a failed-evaluation report, not as release acceptance.** Grain is one planned case / one physical attempt / one distinct completed provider response. All 14 unique case IDs, sequential reservations, response IDs and per-case ledger snapshots reconcile. No missing/unknown provider usage, hidden retry, dropped failed case, duplicate accounting, changed frozen input or repaired native output was found. Costs and durations were independently recomputed; summaries retain the 14-case and five-useful-attempt denominators. UTC timestamps identify the complete collection window.

The runner's raw automatic count (8), audit's recomputed raw automatic count (10) and semantic count (5) are deliberately separated. This is an explained scoring-path difference, not evidence deletion. Raw semantic review checks every fact, including facts outside the minimal golden requirements. A false judgment does not become true because automatic metrics or application refusal pass.

The review uses the data-quality and analysis-validation workflow to check completeness, identity joins, ledger prefixes, units, denominators, full-case timing, source bindings and unsupported interpretations. The durable primary artifacts remain repository JSON/Markdown and byte-identical native outputs rather than a separate analytics dashboard. No notebook, chart, external model-grader or independent reviewer is claimed.

## Publication and verifier review

The final publication adds only approvals, checkpoints, copied evidence, review/results, a read-only portable authenticator, negative-control tests and a separate offline evidence CI workflow. Frozen runner and original workflows are unchanged. The authenticator rejects changed prompts/selections, omitted failed records, ledger reset, unobserved terminal children, modified native artifacts and extra outcomes. Its eight tests pass, as do the 14 frozen runner safeguards. Historical final-01 remains failed at raw 4/14 and application 8/14; old files and ledgers remain unchanged.

The portable verifier reauthenticates all saved failed selections against their ledger entries even when the original child returned nonzero; checks unique case/response identities, exact prefixes/inventory and semantic-review byte bindings; recomputes raw/application scores; and replays both successful bundles against reviewed examples. It does not rerun paid requests or native generators. Its pass means evidence integrity, never that the evaluation passed.

## Required caveats and remaining authority

The small targeted set is not independent or representative. No statistical comparison with older differently worded/contracts batches is justified. Reviewer authorship is implementing-agent. Hashes are local integrity evidence, not cryptographic provider attestations. Complete HTTP-stream archives are unavailable; all saved structured responses and local stdout/stderr are included. No physical, firmware, calibration or fabrication claim is made.

The approved new 14-request budget is now exhausted; cost headroom restores no slots. No more live requests, recovery baseline, new model/schema, Gemini review, fabrication or physical work is authorized by this execution approval. PR remains draft. Further product/implementation work should be separately scoped from this completed failed batch; preserve these exact counterexamples and do not turn the same cases into an alleged unseen holdout.
