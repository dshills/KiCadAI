# Two-family generation milestone

Goal: deliver two distinct reviewed sensor/controller board families through one natural-language-to-KiCad command, with deterministic construction, complete-board validation, no manual evaluation-output repair and consistent native/BOM/preview/Gerber/drill/placement bundles.

Base: merged `main` at `101a96fd1bf095a4b82727508f0a1e72177bc3f7`. Work branch: `codex/board-family-v2`. Preserve the entire earlier board-family and practical-board evidence history; do not reinterpret earlier failures.

Execution order:

1. Restore CI with explicit, hash-bound handling of historical lint-policy conflicts; verify static contracts and coverage merge/floor, not only lint.
2. Inspect reusable blocks and qualify a second genuinely distinct integrated board, engineering references openly before evaluation. Preserve fixed family-specific placement/routing in production.
3. Extend the shared command with explicit family capabilities, configuration validation, family selection and targeted clarification/refusal. Never weaken unsupported requests into supported ones.
4. Export and inspect consistent manufacturing-data bundles for both families; revalidate complete native boards and deterministic replay.
5. Run focused offline tests during development, full bounded integration tests and one compact final live evaluation after a new explicit request/dollar budget is approved.

The previous goal consumed all 41 authorized physical calls. Its ledger stays unchanged. Existing-key preference is retained, but **no live calls are authorized for this new goal yet**. No physical fabrication or bench-performance claim. Do not mark complete before every goal requirement is verified.

## Progress

- Confirmed clean merged base and failed main CI: the same two frozen-source cleanup findings cause the static failure and skip the dependent quality gate.
- Added exact historical lint exceptions following existing repository policy, with pre-lint immutable-source and negative-control tests. This is a disclosed exception, not a repair of historical source. See [CI.md](CI.md).
- Local full-repository lint passes with zero issues. The hash/negative-control and workflow-contract tests pass. Full bounded repository regression passed in 60.967 s (`.cache/board-family-v2/regression-ci-01`), with provider keys removed. The original 677-file evidence audit still passes. Remote CI, including the previously skipped quality gate, remains to be verified.
- Selected SHT31-DIS-B as the second reference candidate, reusing the existing sensor block and installed KiCad symbol/footprint. It adds temperature/humidity measurement with a different sensor, pad/net map and electrical contract. Sensirion's December 2022 v7 datasheet has been inspected; integration and native qualification are not yet complete.
- Opened the one scoped [draft PR #14](https://github.com/dshills/KiCadAI/pull/14). CI run `34770984967` on `be764a48` is now terminal **success: 25/25 jobs**. The formerly skipped quality gate executed and passed: generated-excluded coverage **79.20%**, unchanged floor **75.00%**. Main is not changed by this unmerged PR.
- Added `development/engineer-sht31`, which hash-checks the existing reference, imports the installed sensor symbol/footprint, creates only new development directories, replaces sensor-area wiring explicitly and preserves the controller/support circuit. No general router or model is involved. The fixed sensor is moved away from the controller; one local bypass is retained and redundant C6 removed (16 parts). This is disclosed reference engineering, not repair of evaluated outputs.
- `sht31-reference-01` passed ERC and had zero unconnected nets, but failed clearance/courtyard/metadata checks. Development recipe corrected those issues. Revision 02 had only one unused legacy via; revision 03 removes it and passes ERC, strict DRC, zero unconnected items and zero parity findings. Earlier development outputs remain unchanged.
- `sht31-canonical-01` is a new KiCad-saved copy of revision 03; the same native checks and preview exports pass again. Full A3 schematic and board SVGs were rendered with `rsvg-convert` and visually inspected by the implementing agent. The grouped ESP32 ground labels and incomplete assembly silkscreen remain known limitations. Initial Quick Look square thumbnails cropped the schematic, so they were not accepted as full-sheet review.
- SHT31's documented 300 ns rise-time ceiling requires a distinct profile/capacitance envelope; the old 10 kΩ/50–100 pF profile must not be blindly admitted. Proposed standard 4.7 kΩ/50–70 pF and fast 2.2 kΩ/50–100 pF need implementation/tests and full electrical qualification. All API calls for this milestone remain zero.

## Offline integration checkpoint

- Imported the 19 canonical SHT31 reference assets byte-for-byte using `development/import-reference.mjs`; their identities are in `development/reference-import.json`. No native reference or generated board was manually repaired during integration.
- Shared `Check`/`Generate`/`Validate` now support both families, with the two SHT31 profiles, external-only pull-up sink model, 1.5 mA sensor allocation, correct 16-part BOM and explicit firmware/slew/handling restrictions. Tests bind all nine sensor pads to their intended nets and reject the old low-current profile or excessive loading.
- Added `--list-families` and a two-family AI context/schema. The schema derives each family's allowed profile enumeration from the catalog. Decoder admission tests and historical response replays pass; this is **not** a live language evaluation or proof of semantic selection accuracy.
- A compatibility test discovered that broad directory embedding could capture ignored KiCad `.kicad_prl`/lock files from a developer checkout. Explicit native/library embed patterns now exclude those local files without deleting them. All generated BMP280 project/library/configuration/electrical/BOM bytes match the three published v1 examples.
- The shared validation command now generates Gerber X2, separate PTH/NPTH Excellon, SVG drill maps, drill report and all-component placement CSV only after native checks pass. Export checks bind layer/job metadata, every placement reference/value/package/coordinate/rotation and every drill hit/diameter to the native board; raw export hashes and the source PCB hash are recorded. This does not replace visual export review, assembler orientation checks or fabrication approval.
- `integration-01` verified both SHT31 profiles and BMP280 standard through the original 13 native gates. `integration-02` exposed an incorrect Gerber job/header naming assumption in the new checker; `integration-03`/`04` exposed a half-micron Excellon rounding boundary. Corrected the checker, not the boards or those historical run outputs. The drill comparison now permits only the declared 0.00051 mm serialization tolerance, with precision-boundary and corrupt/missing/duplicate/rotated-coordinate negative tests.
- All five fresh `integration-05` cases pass all **14 native/export gates**, taking roughly **4.2–4.8 seconds per complete local command**. See [authenticated checkpoint](evidence/integration-checkpoint-01.json). This includes successful KiCad save/reload comparisons, zero ERC/strict DRC/parity/unconnected findings and native-input immutability. The checkpoint authenticates all 677 earlier publication files unchanged, as well as current exported bytes. Historical audits that assert current production-source identities remain version-bound; they must not be relabeled as qualification of successor code.
- Focused tests and full-repository lint pass. Full bounded offline regression passed in 37.660 s before the final drill-boundary correction and again in 11.178 s afterward (cached unchanged packages), recorded under `.cache/board-family-v2/regression-integration-01` and `regression-integration-02`. The new integration has not yet been pushed for CI; PR #14's green run still applies only to its earlier checkpoint. API calls for this goal remain zero and the previous live ledger remains unchanged.

## Export validation and review checkpoint

- Reproduced 15 unsafe Excellon admissions in negative controls, then replaced permissive line scanning with a strict KiCad-dialect state parser. Added Gerber declaration/terminator and manufacturing bundle negative controls. No native design or generated export bytes were repaired.
- All five `integration-06` cases and all five independent `integration-07` replays pass all 14 native/export gates. Checkpoints [02](evidence/integration-checkpoint-02.json) and [03](evidence/integration-checkpoint-03.json) bind the same source and binary and verify all 677 old publication files unchanged. Focused tests and full lint pass; full bounded offline regression passed in 38.252 s (`regression-export-01`).
- [Deterministic replay](evidence/deterministic-replay-01.json) passes for 199 deliverable files over five configurations. Native designs, libraries, configuration and BOM/electrical files are byte-identical per configuration. Only explicitly named native timestamps are normalized for preview/manufacturing comparisons; raw files and hashes remain intact. Six normalization tests reject missing/duplicate dates and preserve geometry/body differences.
- Independently rendered and visually inspected both families' actual native previews, top/bottom Gerbers, copper/drill overlays, paste/mask layers, sensor details, outlines and drill maps. Inspected placement data and bound every populated row through native checks. The review recipe uses isolated, pinned tooling with hashes and retained warnings. Early renderer setup/API/color-map failures and blank outputs remain recorded and were not accepted as visual review.
- The 15 non-placement manufacturing deliverables are timestamp-normalized-identical across each family's profiles, allowing explicit review reuse. See [manufacturing review and caveats](MANUFACTURING_REVIEW.md) and [image/source identities](evidence/visual-review-01.json). These are software-export checks, not fabrication approval, stencil/process qualification or independent engineering review.

Next: finish consolidated SHT31 electrical/assembly qualification notes and publish consistent examples. Then freeze one compact live acceptance batch, obtain its new request/dollar budget, run it once, finish review and update the existing draft PR #14 with fresh CI. No API calls are authorized or consumed for this milestone. Do not mark the goal complete from these offline checks alone.

## Assembly-reference correction and example publication

- Visually checked Sensirion's actual land/stencil drawing and handling pages. The original center paste aperture was already reduced, but the perimeter paste offset and mask clearance/grouping needed correction. Added an openly engineered, distinctly named custom footprint with unchanged copper, nets, routing and placement. The intentional shared mask attribute is local to U2; board-wide DRC policy is unchanged.
- Added a reproducible development recipe and tests for all eight paste centers/sizes, both mask rows, exposed-pad clearance, unchanged copper/input/library data, unexpected-input rejection and captured-reference equality. `assembly-01` failed symbol-library parity; the recipe now updates only the placed schematic instance. `assembly-02`/`03` passed ERC/DRC but import byte checking and then generated native round-trip checking exposed terminal-newline and canonical syntax/order differences. Those failed attempts remain recorded. `integration-08` exports were withheld; no evaluated output was edited and no checker was relaxed.
- `sht31-assembly-04` passed ERC, strict DRC, unconnected and schematic-parity checks and was imported with all 20 reference-file hashes verified. All five fresh `integration-09` cases and all five `integration-10` replays pass all 14 gates, in roughly 3.75-3.99 seconds per local command. Checkpoints [04](evidence/integration-checkpoint-04.json) and [05](evidence/integration-checkpoint-05.json) authenticate the unchanged 677-file v1 history. [Replay 02](evidence/deterministic-replay-02.json) compares 201 deliverables successfully.
- Finished the [consolidated electrical/assembly-data review](ELECTRICAL_ASSEMBLY_REVIEW.md). It distinguishes nominal controller logic-table calculations from full-envelope guarantees, and family engineering limits from sensor ratings. Assembly, power integrity, firmware and ambient accuracy remain explicitly unmeasured. Inspected 15 fresh SHT31 images plus actual placement CSV; reused BMP280 review only after byte/content equivalence to the new replay. [Visual review 02](evidence/visual-review-02.json) binds both families and the reviewed notes.
- Published [five complete example bundles](../../examples/board-family-v2/README.md), including reviewed manufacturing exports, with 256 copied files authenticated byte-for-byte. Existing examples and frozen evidence were not changed. Original execution paths/timestamps remain as provenance, not a claim of revalidation after relocation. [Publication receipt](evidence/examples-01.json).
- Focused tests, full-repository lint (zero issues), and full bounded offline regression passed; the latter took 40.402 seconds with provider keys removed (`regression-assembly-01`). No API calls were made; the exhausted v1 ledger remains unchanged. These changes are not yet pushed to PR #14, so its earlier green CI does not cover them.

Next: freeze the compact final two-family language evaluation and obtain its new request/dollar budget; then complete acceptance, final review and fresh PR CI. Physical fabrication and bring-up remain separate and unauthorized. The goal remains active.

## Final language batch prepared, not executed

- Committed the reviewed reference and five example bundles at `43573273`. Git whitespace checking passes for authored source/review files. Its full-artifact check reports native blank EOF lines and upstream SVG/log whitespace; those raw generated bytes are intentionally retained and hash-authenticated, not reformatted to silence that check.
- Froze [14 targeted cases](evaluation/cases-01.json), [protocol 01](evaluation/PROTOCOL-01.md) and the unchanged exported payload in [freeze 01](evaluation/freeze-01.json). Five supported configurations match the published examples exactly; three cases require targeted clarification and six require refusal. Prompts include negations, mixed requirements and contradictions. They are implementing-agent-authored acceptance checks, not independent holdouts.
- Proposed authorization is 14 physical calls and USD 1.00 total using the existing key. No approval or call is represented by these files. Before execution, the remaining budget plumbing must enforce a separate v2 goal policy, pass offline tests and bind the final execution binary while keeping the frozen payload/cases and exhausted v1 ledger unchanged. The goal remains active; approval is still needed.
