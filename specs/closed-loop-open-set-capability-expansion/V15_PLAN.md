# V15 Plan

1. Authenticate and permanently retire the incomplete V14 generation-zero run.
2. Replace the all-failures graph list with deterministic online best-per-
   topology retention, and stop topology-repair sizing generation at its
   existing ordered limit. Stream canonical repair hashing without a complete
   encoded buffer while preserving repair selection, hashes, and output.
3. Freeze the V15 bounded-failure, single-worker, post-replay-release evaluator
   and bind the corrected production synthesis source.
4. Run local equivalence, resource, regression, lint, and external staged
   review, then commit the freeze.
5. Execute a fresh 24-case discovery baseline under the committed environment.
6. Authenticate and publish only a complete deterministic report.
7. Cluster typed failures, rank generic gaps, and implement only the selected
   generic capability before public and authorized blind held-out comparison.
