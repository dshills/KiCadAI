# V12 Public Baseline Protocol

Evaluate all 24 public discovery cases under the frozen V12 evaluator and
environment. Every case requires two byte-identical synthesis replays,
complete route and evidence gates, and installed-KiCad promotion wherever the
frozen policy requires it.

Only a complete deterministic report may be atomically published. Partial
checkpoints are never accepted evidence. The report remains public discovery
evidence and must not read any held-out key or encrypted held-out record.
