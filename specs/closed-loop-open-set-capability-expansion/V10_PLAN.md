# Closed-Loop Open-Set Capability Expansion V10 Plan

## Phase 0: Freeze the corrective contract

- Bind the V9 prepublication retirement and `held_out_source_opened=false`.
- Freeze this addendum, plan, corpus rules, baseline protocol, exact schemas,
  budgets, keys, and terminal states.
- Checksum and Prism-review the exact contract; remediate every high/medium
  finding before commit.

## Phase 1: Prove assignments and freeze author packets

- Generate six disjoint eight-case assignments from metadata only.
- Run the production assignment-feasibility preflight before writing packets.
- Independently reproduce the proof from committed packet bytes.
- Freeze per-author and packet-set checksums only after every quota and
  high-safety domain/role coverage invariant passes.
- Dispatch fresh authors only after separate explicit authorization.

## Phase 2: Validate and publish the corpus

- Validate 48 fresh behavior-only requirements without synthesis.
- Adjudicate public electrical meaning and bounded aggregate omissions only.
- Create a new external 0600 V10 source key and atomically publish 24 public
  plus 24 record-encrypted held-out requirements.
- Authenticate every record before removing any V10 quarantine plaintext.

## Phase 3: Freeze and run generation zero

- Freeze evaluator, inventory, models, simulation policy, installed KiCad,
  seeds, concurrency, locale, and resources.
- Evaluate all 24 discovery cases twice; promote every pass twice.
- Publish public evidence, outcome counts, obligations, and typed root paths.
- In isolation, create a distinct 0600 baseline key and publish encrypted
  held-out baseline evidence plus non-revealing aggregates.

## Phase 4: Rank and seal the highest-impact generic capability

- Enumerate complete candidates and their closure/exposure cohorts.
- Rank by unlock/diversity first and collateral exposure/sibling burden next.
- Publish the full semantic co-rank-one set and deterministic fallback.
- Freeze selected paths, siblings, non-exposed cases, effect plan, production
  paths, tests, and before hashes before implementation.

## Phase 5: Implement and verify

- Modify only generic mapped production and verification paths.
- Add representative, boundary, adversarial, deterministic, fail-closed, and
  sibling-preservation tests without inspecting V10 outcomes.
- Run focused/full local tests, vet/lint, all historical regressions, and all
  installed-KiCad protected fixtures.
- Prism-review, remediate, commit, and freeze the implementation seal.

## Phase 6: Exactly-once advancement and blind final

- Run all public discovery cases exactly twice under the committed seal.
- Atomically publish admission, bounded continuation, or retirement.
- After public admission only, authorize one isolated final custodian using a
  third distinct 0600 key.
- Authenticate the final artifact, update durable documentation, Prism-review,
  commit, and push. Never manually run GitHub Actions.
