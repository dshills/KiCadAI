# Diagnosis-Driven Repair Plan

1. Freeze the untouched electrical and physical cases, base commit, source
   hashes, initial outcomes, and deterministic replay evidence.
2. Add one identity-neutral repair trace schema shared by electrical and
   physical loops.
3. Normalize simulation diagnoses into bounded electrical repair categories;
   record every graph proposal, stage re-entry, expected effect, outcome, and
   safe rejection.
4. Normalize placement/routing issues and route-operation correlation into the
   same evidence contract; record placement versus routing re-entry.
5. Prove the protected current driver either promotes or fails closed with a
   more precise stable gap under fixed budgets.
6. Prove a real blocked six-component, twelve-pad, multi-endpoint route case
   recovers deterministically without design-identity dependence.
7. Run focused and full local preservation lanes, installed-KiCad gates,
   genericity/content audit, Prism review, remediation, and commit.
