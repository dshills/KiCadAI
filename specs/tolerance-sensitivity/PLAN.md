# Delivery plan

1. Add immutable bounded uncertainty evidence to resolved simulation plans.
2. Enumerate deterministic nominal, one-at-a-time, and complete endpoint
   corners with a fixed computational bound.
3. Re-evaluate every assertion, emit dominant sensitivity attribution, and
   fail closed on invalid evidence or failed corners.
4. Propagate verified passive catalog tolerances through both binding and MNA
   resolution without changing nominal workflows.
5. Extend the frozen tolerance corpus and capability report, then run full
   KiCad promotion gates before release.
