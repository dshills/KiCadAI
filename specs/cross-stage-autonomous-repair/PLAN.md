# End-to-End Cross-Stage Autonomous Repair Plan

1. Freeze the independent nine-stage corpus and its initial blocked evidence.
2. Add the versioned cross-stage diagnostic, proposal, checkpoint, gate,
   margin, trial, budget, and report model with canonical validation and hashing.
3. Implement the deterministic coordinator: earliest-stage selection, bounded
   trial ordering, checkpoint restore, candidate ranking, confirmation, and
   stable stop reasons.
4. Add preservation and regression comparison for unrelated scope, required
   gates, operating corners, thermal margins, SOA margins, and physical gates.
5. Adapt electrical causal repair, schematic/ERC correction, placement/routing
   correction, connectivity repair, persisted DRC repair, writer correction,
   and round-trip correction to the shared proposal/application boundary.
6. Recover each independently frozen case and add mixed-stage ordering,
   rollback, cancellation, budget, and byte-identical replay tests.
7. Promote all applicable cases through the complete local installed-KiCad
   workflow twice and compare content-addressed artifacts.
8. Run preservation lanes, update README/project status/roadmap, complete a
   requirement-by-requirement audit, review with Prism, remediate, and commit.
