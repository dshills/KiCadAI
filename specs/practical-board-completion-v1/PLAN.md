# Bounded end-to-end completion plan

Status: preparation; no production changes, development batch or live request at plan creation. Authority: [AUTHORIZATION.md](AUTHORIZATION.md). This plan targets the complete original practical-board goal, not another two-example layout milestone.

## Evidence and implementation workstreams

1. **Faithful requirement handoff and comparison.** Current-source diagnosis retains 12/12 invalid non-reserved baseline proposals. The repaired provider contract has not yet demonstrated fresh live fidelity on these boards. Build a strictly decoded, source-bound accepted-requirement handoff with independent clause review and exact paired engine inputs. Preserve refusal/clarification ownership and response/usage integrity. Never repair a provider proposal into an expected circuit answer.
2. **Complete multifunction engine integration.** The frozen old evaluator does not select the newer opt-in schematic policy. Implement a separate experimental adapter linking each engine's real architecture search, trusted electrical synthesis, normal board workflow, native validation and complete replay evidence. No baseline production patch, precomputed circuit, manually selected component, synthetic library or case-specific route. Keep old evaluator bytes frozen. Admission must cover the substantive functions and required operating/mechanical conditions of the actual non-reserved briefs; existing simple fixtures alone cannot justify six complete boards.
3. **Readable functional drawings.** The ownership-v6 pair retains 0/2 complete readability despite native correctness. Address evidenced generic label/field collisions and functional interface/power/return organization; keep net/pin identity, electrical conditions, board dimensions, routing and safety unchanged. Use independent regressions and the unchanged full readability rubric, not a newly convenient drawing metric.

No other production workstream is implicitly authorized. A needed component/model addition requires manufacturer-backed provenance and independent qualification inside workstream 2; unsupported substitutions remain failures.

## Fixed development and execution limits

- At most **three development batches**, each with a preregistered immutable manifest of at most six non-reserved or separately authored synthetic requirements. Record every selected input and its SHA-256 before the first batch; retain the same denominator across batches. P07/P08 cannot be development inputs. Every search/electrical/native result is retained, including stopped/not-run stages. A batch cannot be split into unrecorded retries.
- Each development case: 20 minutes including two downstream runs; each batch: two hours; serial execution, GOMAXPROCS=4, sampled process-tree RSS 16 GiB, retained raw tree 10 GiB. Keep at least 10 GiB free, require 12 GiB before dispatch. Preserve originals; no pruning failures to meet limits. Unit/regression checks and non-executing snapshots are recorded separately and are not board attempts.
- Before live dispatch, require an explicit source-bound readiness review of the full target: all non-reserved functional/condition categories mapped to tested generation and verification paths, complete native/replay controls, full visual rubric on the fixed development set, and a credible path to 6/8 positives including the reserved target without inspecting/tuning those reserved details. If readiness fails after the fixed development allowance, publish the negative result without spending the live allocation or starting another cycle.
- After readiness and source/evaluator/environment/input freeze, permit **one** final live campaign (36 POSTs / USD 43.35 maximum). Both positive engines get the same exact accepted requirement bytes, not edited copies. Original 20-minute case / two-hour campaign, 16 GiB RSS / 10 GiB evidence limits remain. No final-source repair, native replay restart or second final to improve the result.
- Fixed provider: `gpt-5.6-sol`, Responses, strict structured output, streaming, background false, store false, no tools/uploads, maximum 16,384 output tokens, 131,072 encoded request bytes, 2 MiB response limit, five-minute transport deadline, one initial plus at most one compiler-diagnostic correction per logical input. No automatic or uncertain transport retry. No paid access probes before freeze; the first planned case establishes access. Verify pricing and effective service tier before freeze; do not silently substitute settings.
- Maintain a create-exclusive campaign journal. Reserve cost before dispatch, reconcile only supported usage, retain maximum reservations for unknown usage and stop before any request that cannot fit all remaining caps. Carry historical accounting by authenticated reference without mutating the old journal. No API key in process arguments, evidence, logs or Git.

## Verification and publication

Freeze the adapter tests, strict gate names, normalization, same-input compatibility rules and source identifiers before the final. A decoder error or missing wrapper API is a protocol incompatibility, not a qualifying engine uplift. A counted improvement needs faithful semantics, a material baseline engine failure and a complete candidate pass, independently reviewed against the original clauses. Report compiler, engine and final AI-facing results separately.

Before final freeze, run affected unit/race regressions, original evaluator/CLI race tests, Node evidence tests, repository vet and command/internal lint. Run the complete short Go suite once on final source with the unchanged 12-minute per-package limit; preserve any failure. Historical race timeouts remain disclosed unless the corresponding complete current-source command actually passes.

Seal original project trees before exporting from disposable copies; inspect whole sheets, dense groups and all copper layers. Keep exact project identity and normalized full evidence comparisons separate. Every required gate must be present and successful; unavailable, skipped or partial evidence fails. Authenticate all attempts and archives without replacing history. Update capability documentation and open a reviewed PR with full limitations; no merge/release/fabrication. Local self-review is disclosed; this approval does not silently authorize a new Gemini upload or another reviewer identity.
