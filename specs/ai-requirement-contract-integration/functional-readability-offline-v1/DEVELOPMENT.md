# Retained development record

All commands use the bounded key-free runner in [run.mjs](run.mjs). No provider calls occurred. Every test execution has an exclusive log, execution receipt and exact changed-Go-source snapshot.

| Attempt | Result | Interpretation |
| --- | --- | --- |
| focused | Failed | The first unit check used Go structural equality on a JSON round trip, including empty versus omitted slices. It was corrected to compare serialized layout fields. The later native comparison remains byte-exact, with no replay normalization. |
| focused-dev2 | Passed | Ownership derivation, layout-only mutation, clone isolation, serialized fields and invalid/missing/duplicate provenance cases. |
| native-dev1 | Regulator passed; controller blocked | Group-local layout retained three-high capacitor columns. Controller project writing rejected `composition_net_003` at 176.53, 463.55 mm because the native annotation search found no wire/body-clear candidate. Later required stages were skipped, not passed. The regulator's two native runs and both examples' attempted outputs are retained. |
| native-dev2 | Both examples passed twice | Larger local spacing and one-high support rows avoided the collision. Rendered regulator rows were too wide, and the controller remained tall. |
| focused-dev3 | Passed | Same focused tests after bounded layout changes. |
| native-dev3 | Both examples passed twice | Pure decoupling blocks use two-high rows with a centered active device, and wider block-row packing gives a landscape controller sheet. Individual support distances still regress. |
| lint, lint-final, focused-final | Passed | Lint reported zero issues; final focused tests passed on frozen source. |
| native-final | Both examples passed twice | The single final run completed in 30.978 seconds for the package. All nine strict stages passed for all four runs. No later native evaluation or source repair. |
| race | Timed out; overall failed | `compositionlowering` reached the unchanged 12-minute limit in `TestBehavioralCorpusResolvesAndExecutesTrustedAnalysisPlans/mixed_function_control_power`; the stack is in nonlinear simulation. `schematicir` passed in 236.018 s and `schematiclayout` in 3.157 s. No race warning was emitted before the timeout, which is not a race-clean conclusion. No retry or timeout extension. |
| vet | Passed | `go vet ./...` completed on the frozen source. |
| full | Passed | All 151 test packages passed, with 14 packages having no tests. Longest: `opentopologysynthesis`, 675.774 seconds, below the unchanged 720-second package timeout. |

The first history-authentication helper invocation used the newer archive receipt filename for the older native-readability phase and stopped at an absent file. Its read-only checks did not alter history. The helper was corrected to use that phase's actual `archive-verification.json`, and the complete history check passed. A process-list diagnostic was denied by the sandbox; the bounded test sessions were observed through their own logs instead. A requested lower CPU priority for archive creation was denied; the same archive command proceeded at normal priority and succeeded once.

Three development native evaluations and one final evaluation consumed the full preregistered allowance. Their raw roots are `/tmp/kicadai-functional-readability-offline-v1-{dev1,dev2,dev3,final}`. No earlier raw directory, archive, result or source snapshot was edited or deleted. Source snapshots are retained under `.cache/functional-readability-v1-sources` and included in this phase's archive.

The first final-report QA invocation stopped because README linked to the QA receipt that the checker itself had not yet created. The checker now permits only its own pending output during the initial link pass and verifies that every link exists after publication. No native evaluation, Go source, or sealed evidence changed.
