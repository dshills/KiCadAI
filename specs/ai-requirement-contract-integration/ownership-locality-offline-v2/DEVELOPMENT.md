# Development record

The [plan](PLAN.md) was written before implementation. Provider keys were removed from test subprocesses; cached Go dependencies used `GOPROXY=off` and `GOSUMDB=off`. No provider API was called.

1. Added fail-closed lowering for unknown, cyclic, duplicate and fragment-conflicting support parents. A component with an unresolved recorded parent may no longer fall back to an external boundary role. Added source/parent consistency validation for the new ownership-v2 profile; ownership-v1 retains its previous source-string compatibility.
2. Added opt-in ownership-v2 support placement. Recorded support parents take priority. A fragment decoupler without a support-parent record may use its group's unique active component; multi-active ambiguity is left unchanged. A bounded deterministic radial search respects transformed symbol/pin-label envelopes and clearance. Search exhaustion or cycles supplied directly to the layout engine return the original block positions, without partial movement. Existing fixed-component requests bypass functional rearrangement.
3. First focused tests passed. Added explicit connector cases, source consistency tests and a no-parent fast path; second focused tests passed. Lint reported zero issues.
4. Native development run 1 passed both unchanged examples and their actual JSON-decoded replays. Sealed projects were exported from disposable copies. Independent verification confirmed all nine strict stages, zero ERC/DRC findings, exact primary-file replay, physical connectivity and unchanged PCB geometry. Decoupler mean/maximum distances improved with no individual decoupler regression. Full-sheet inspection still found opaque net names, remote guide text and weak functional reading. Controller reset C10 and pull-up R3 moved slightly farther from their parent; these are not decouplers and are reported separately, not omitted.
5. Added a concurrent shared-input test for explicit multi-active parent selection and chained support placement. Final focused tests passed. No drawing implementation changed after native development run 1. The final snapshot includes 11 changed Go files, including the native replay test. The second permitted development native run was not used.
6. Froze Go source and ran one final native evaluation, one full short suite (12-minute package bound), and the planned race partitions. Final native validation passed, with requests and all primary project files exactly equal to development run 1. Both scoped race partitions, vet and lint passed. The full short suite passed 151 packages (14 without tests); its longest package finished in 690.673 seconds. There were no Go source edits or further native attempts after the final run began. All outcomes are retained in the execution receipts and final report.

Atlas structural lookup and summaries guided source navigation. Its stale-package report includes unrelated historical package-summary entries; the exact changed-file snapshots and Git diff, rather than stale summaries, authenticate the evaluated source. Prior source/raw/archive evidence and the original campaign chain were re-authenticated without modification.

Final staged-file QA found one trailing space in the new archive helper, which the earlier unstaged-only check did not include. The whitespace was removed and the staged check added to QA. An initial patch attempt matched an incomplete Markdown paragraph and was rejected without changes; the corrected patch applied. These were helper/report-only corrections: frozen Go source, native outputs, test logs, source snapshots and archive bytes were unchanged. No test or native evaluation was rerun.
