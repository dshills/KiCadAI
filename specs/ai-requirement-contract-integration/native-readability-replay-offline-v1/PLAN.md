# Native readability and serialized replay: approved offline follow-up

Approved 2026-09-11 after the retained negative readability result. Base commit: `ec2ececb4e52643f0767d0fcbb70213f1ec7209c`. Local branch: `codex/native-readability-replay`.

## Required outcome

Resolve the evidence-backed native annotation collisions and lost serialized layout state. Make functional blocks, connector purpose, power/reference intent and signal flow understandable through annotations derived from existing explicit contracts, without renaming electrical nets or inventing requirements. Preserve the experimental/legacy policy boundary and reject unknown policy versions. Native readability must be checked against emitted geometry; internal planner success alone is insufficient.

## Invariants and authorization

Use the unchanged standalone regulator and separately recorded controller ADC 100 mA requirements. Preserve all existing electrical components, nets, constraints, board placements/routes and the original 150 mA thermal rejection. No hand-editing generated designs. Preserve all earlier commits, reports, frozen inputs, evaluator binaries, raw outputs and archives. No API/provider call, new live campaign, external review, push, PR, merge, release or fabrication is authorized.

These are two development examples, not frozen positive passes. The full six-positive/two-paired-design milestone remains unchanged and unproven.

## Verification

1. Add failing regressions for serialized inferred-layout state and the observed native label/field/wire collisions; implement reusable corrections.
2. Verify unchanged electrical/PCB intent, legacy receipts, and versioned policy behavior with focused tests. Run applicable race checks, lint/vet and the full repository short suite with a 12-minute per-package bound, using cached dependencies and provider keys removed.
3. Generate each unchanged example with installed native libraries and strict routing, simulation, writer round-trip, ERC and DRC. Generate a second time from an actual serialize/deserialize workflow request, not the same in-memory object. Bound native generation to 20 minutes per example.
4. Seal all generated project files before exports, render only disposable copies, inspect every sheet and dense regions at documented scale, verify exact physical pin/pad connectivity, and recheck original seals. Do not normalize away geometry or net differences in serialized replay.
5. Bind final source and raw evidence; retain development failures and exact source snapshots for this phase. Authenticate earlier evidence without rewriting old receipts. Record local review, results and limitations.

If final verification exposes another downstream failure, retain and publish it without weakening acceptance or silently beginning another repair/evaluation phase.
