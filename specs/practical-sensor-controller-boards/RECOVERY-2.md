# Approved recovery baseline 2

The user explicitly approved one additional recorded recovery baseline on
2026-09-11, using the existing key, unchanged frozen evaluation and remaining
original budget. This record precedes dispatch. It authorizes recovery 2 only,
not another automatic retry or recovery cycle.

## Immutable lineage

The original interrupted baseline and recovery 1 remain intact, including both
local archives, all original receipts and their published reports. Before this
record, all 18 frozen files, all 25 original raw files and all 28 recovery-1 raw
files authenticated again. Neither interrupted campaign has a design score.

Recovery 2 uses the new root
`/tmp/kicadai-practical-sensor-controller-public-1-recovery-2`.
Its journal receives byte-identical copies of receipts 001 and 002 from the
recovery-1 journal. These remain two historical reservations, not new calls.
The first newly reserved call will be 003. Subsequent originally planned
final/paired work, if justified, must use this recovery cohort and cumulative
journal.

## Evaluation identity and network state

Reuse the exact existing binary
`/tmp/kicadai-practical-board-eval-recovery-1`, SHA-256
`45e6c24392f1ba3469e5a6b9d4cd6857eb617bfa39de59c1ee87294660fe0ee3`.
Its filename does not identify the new cohort: the distinct evidence root,
authorization and journal do.

The binary's clean build came from commit
`4341df799c25c811cd05e80f5d860c009ea50585`.
A detached checkout at `/tmp/kicadai-practical-recovery-2-source` matches that
source identity. Later current-branch commits only add recovery reports and
post-freeze read-only evidence authentication; no production or frozen evaluator
bytes differ. The unchanged supervisor verifies the clean source/build,
merged-V23 production identity, tool versions/hashes, installed native libraries
and policy before any live case.

A read-only LuLu check still finds the existing Allow rule for this exact worker
path, created after recovery 1 ended. No firewall setting or rule is changed.
Reusing the same executable preserves its prior evidence identity and avoids
building a different executable requiring a new rule. No post-rule provider
response has yet established authentication or model access.

## Unchanged bounds and stopping rule

The fixed model remains gpt-5.6-sol with the original request configuration,
correction allowance, time/memory/evidence caps and acceptance gates. All
eight positives, four refusals, two clarifications and two paraphrases remain.

Two historical reservations retain USD 0.912192; this is an estimate/reservation,
not verified billing. Remaining headroom is 34 baseline requests, 70 total
requests and USD 49.087808 estimated spend. Absolute limits remain 36 baseline /
72 total requests and USD 50, including carryover.

Keep both prior interrupted cohorts separately visible. If recovery 2 completes,
use its authenticated outcomes for the baseline scope decision; do not claim
uninterrupted first-workflow success. No production fixes are selected before
that decision, and the public-frozen reserved cases cannot drive tuning.

A terminal provider or resource failure stops this recovery. No further recovery,
acceptance change, model change, firewall bypass, merge, release, fabrication
approval or support-boundary expansion is authorized by this approval.

Machine-readable authorization: `recovery-2-authorization.json`.
