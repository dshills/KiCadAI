# Approved recovery baseline 1

Authorization recorded 2026-09-11, before dispatch: the user explicitly said,
"I approve the separately recorded recovery baseline" in response to a request
for one new recorded baseline using the existing key, unchanged frozen tests
and model, and the remaining original request/estimated-spend budget.

This is a narrow, disclosed exception to the original no-rerun rule after the
first campaign failed during TCP connection establishment without an HTTP
response. It does not change any frozen evaluator or acceptance bytes, allow
outcome-driven tuning, or authorize another recovery. TCP connectivity was
checked without credentials or an API request and now succeeds; authentication
and model access remain unproven until an actual recorded API response arrives.

## Evidence and budget continuity

- Original interrupted evidence remains immutable at
  `/tmp/kicadai-practical-sensor-controller-public-1`, with its verified local
  archive retained. Its 25-file inventory and all 18 frozen files authenticated
  again before this authorization record. The production tree still matches
  merged V23. Pinned Go, Node and KiCad identities match the original freeze.
- Recovery uses the distinct root
  `/tmp/kicadai-practical-sensor-controller-public-1-recovery-1`.
- Copy the two original request-journal receipts byte-for-byte into the new
  journal before dispatch. They remain the same original reservation, not two
  newly counted files or another request. Verify their hashes after copying.
- The recovery journal becomes the cumulative journal for recovery and any
  subsequent originally planned final run. It already counts the first
  reservation and USD 0.456096 retained estimate. Maximums stay 36 baseline
  requests including carryover, 72 overall, and USD 50 estimated/reserved spend.
  Consequently, no more than 35 new baseline requests, 71 new total requests,
  or USD 49.543904 of remaining estimated/reserved spend are available.
- The unchanged supervisor controls all cases, correction attempts, time,
  sampled memory, evidence retention, environment checks and terminal errors.
  Its `baseline` phase name is disambiguated by the recovery root and this
  authorization record. No prior directory is overwritten or reopened.

## Interpretation and stopping rule

The original interrupted run remains visible and has no design-capability
score. If complete, this explicitly approved recovery cohort supplies the
baseline for the later final/paired comparison. Report recovery first-attempt
and correction outcomes as recovery outcomes, never as an uninterrupted first
workflow. Do not double-count the carried receipt when aggregating old and new
directories. The eight-positive denominator, public-frozen reserved cases,
four refusals, two clarifications and two paraphrases remain unchanged.

No additional implementation is selected yet. Once the recovery baseline is
authenticated, apply the original baseline scope gate before any production
edits. If no credible bounded route to two distinct complete uplifts exists,
report that finding and request a new scope. A further transport failure is
terminal under the unchanged supervisor; this approval is not permission for
an unlimited recovery loop.

`recovery-1-authorization.json` supplies the machine-readable authorization,
lineage and remaining-budget record. The original SPEC, corpus, scoring,
feasibility and evaluator remain sealed by the original `freeze.json`.
